![Снимок экрана от 2025-04-25 12-30-01](https://github.com/user-attachments/assets/c56cea47-f371-420a-808e-01578fccd718)﻿## Part 1. Установка ОС

- **Действия:**
  - Установлена Ubuntu 20.04 Server LTS без графического интерфейса в VirtualBox.
- **Версия Ubuntu:**
  - ![Скриншот вывода команды `cat /etc/issue`](![Снимок экрана от 2025-04-25 12-30-01](https://github.com/user-attachments/assets/fa22a9fa-eff4-4de3-9149-1935d0d3ba51)
)

## Part 2. Создание пользователя

- **Действия:**
  - Создан новый пользователь `<имя_пользователя>` и добавлен в группу `adm`.
- **Создание пользователя:**
  - ![Скриншот вызова команды для создания пользователя](#вставить_скриншот)
- **Проверка в /etc/passwd:**
  - ![Скриншот вывода команды `cat /etc/passwd`](#вставить_скриншот)

## Part 3. Настройка сети ОС

- **Переименование машины:**
  - Название машины установлено в `user-1`.
- **Установка часовой зоны:**
  - Установлена временная зона `<ваша_часовая_зона>`.
- **Сетевые интерфейсы:**
  - ![Скриншот вывода команды списка интерфейсов](#вставить_скриншот)  
  - _Объяснение наличия интерфейса `lo`._
- **IP от DHCP:**
  - ![Скриншот получения IP от DHCP](#вставить_скриншот)  
  - _Расшифровка DHCP: Dynamic Host Configuration Protocol — протокол автоматической настройки сетевых параметров._
- **Внешний и внутренний IP шлюза:**
  - Внешний IP шлюза: `<внешний_ip>`
  - Внутренний (по умолчанию) IP шлюза: `<внутренний_ip>`
- **Статические настройки:**
  - IP: `<статичный_ip>`
  - Gateway: `<статичный_gw>`
  - DNS: `1.1.1.1`, `8.8.8.8`
  - ![Скриншот статических настроек после перезагрузки](#вставить_скриншот)
- **Пинг удалённых хостов:**
  - ![Скриншот вывода `ping 1.1.1.1` и `ping ya.ru` с 0% потерь](#вставить_скриншот)

## Part 4. Обновление ОС

- **Обновление пакетов до последней версии:**
  - Выполнена команда `sudo apt update && sudo apt upgrade -y`.
- **Проверка отсутствия обновлений:**
  - ![Скриншот сообщения о том, что обновления отсутствуют](#вставить_скриншот)

## Part 5. Использование команды sudo

- **Разрешение sudo для пользователя:**
  - Пользователь `<имя_пользователя>` добавлен в группу `sudo` или внесён в `/etc/sudoers`.
- _Назначение команды sudo: позволяет выполнять команды от имени суперпользователя с проверкой прав и логированием._
- **Изменение hostname от имени пользователя:**
  - ![Скриншот с новым hostname](#вставить_скриншот)

## Part 6. Установка и настройка службы времени

- **Настройка автоматической синхронизации:**
  - Включён и запущен сервис `systemd-timesyncd` (или `ntp`/`chrony`).
- **Текущее время и часовой пояс:**
  - ![Скриншот вывода `timedatectl`](#вставить_скриншот)
- **Проверка NTPSynchronized=yes:**
  - ![Скриншот вывода `timedatectl show`](#вставить_скриншот)

## Part 7. Установка и использование текстовых редакторов

- **Установка редакторов:**
  - Установлены VIM, NANO, MCEDIT.
- **Создание файлов и сохранение:**
  - VIM: ![Скриншот `vim test_vim.txt` с никнеймом перед закрытием](#вставить_скриншот)  
    _Действие для сохранения и выхода: `:wq`._
  - NANO: ![Скриншот `nano test_nano.txt` с никнеймом перед закрытием](#вставить_скриншот)  
    _Действие: `Ctrl+O`, `Enter`, `Ctrl+X`._
  - MCEDIT: ![Скриншот `mcedit test_mcedit.txt` с никнеймом перед закрытием](#вставить_скриншот)  
    _Действие: `F2` (Save), `F10` (Exit)._
- **Редактирование без сохранения:**
  - VIM: ![Скриншот после редактирования в VIM без сохранения](#вставить_скриншот)  
    _Действие: `:q!`._
  - NANO: ![Скриншот после редактирования в NANO без сохранения](#вставить_скриншот)  
    _Действие: `Ctrl+X`, при запросе `N`._
  - MCEDIT: ![Скриншот после редактирования в MCEDIT без сохранения](#вставить_скриншот)  
    _Действие: при запросе сохранить `No`._
- **Поиск и замена:**
  - VIM: ![Скрин поиска слова в VIM](#вставить_скриншот)  
    Команда поиска: `/<слово>`  
    Замена: `:%s/old/new/g`.
  - NANO: ![Скрин поиска в NANO](#вставить_скриншот)  
    Поиск: `Ctrl+\\`  
    Замена: `Ctrl+\\` и ввод новых данных.
  - MCEDIT: ![Скрин поиска в MCEDIT](#вставить_скриншот)  
    Поиск и замена через `F9` (Search) и `F9` (Replace).

## Part 8. Установка и базовая настройка сервиса SSHD

- **Установка SSHd:**
  - `sudo apt install openssh-server`.
- **Автостарт:**
  - `sudo systemctl enable ssh`.
- **Порт 2022:**
  - Изменён `Port` в `/etc/ssh/sshd_config`.
- **Проверка процесса sshd:**
  - ![Скриншот вывода `ps aux | grep sshd`](#вставить_скриншот)  
    _Объяснение: `ps aux` — `a` все пользователи, `u` формат с владельцем, `x` без терминала._
- **Перезагрузка и проверка прослушивания порта:**
  - ![Скриншот `netstat -tan | grep 2022`](#вставить_скриншот)  
    _Ключи `-tan`: `t` TCP, `a` все, `n` числовые адреса; 0.0.0.0 — слушает на всех интерфейсах._

## Part 9. Установка и использование утилит top, htop

- **Установка и запуск:**
  - `sudo apt install htop`.
- **Данные из top:**
  - **uptime:** `<значение>`  
  - **Авторизованные пользователи:** `<число>`  
  - **Средняя загрузка:** `<load averages>`  
  - **Общее число процессов:** `<число>`  
  - **Загрузка CPU:** `<%>`  
  - **Загрузка памяти:** `<%>`  
  - **PID процесса с наибольшей памятью:** `<pid>`  
  - **PID процесса с наибольшей загрузкой CPU:** `<pid>`
- **Скриншоты htop:**
  - Отсортированный по PID: ![...](#)
  - По PERCENT_CPU: ![...](#)
  - По PERCENT_MEM: ![...](#)
  - По TIME: ![...](#)
  - Отфильтрованный для sshd: ![...](#)
  - Процесс syslog, найденный через поиск: ![...](#)
  - Добавлены hostname, clock, uptime: ![...](#)

## Part 10. Использование утилиты fdisk

- **Вывод fdisk -l:**
  - ![Скриншот вывода `fdisk -l`](#вставить_скриншот)
- **Данные о диске:**
  - Название диска: `<название>`  
  - Размер: `<размер>`  
  - Количество секторов: `<число>`  
  - Размер swap: `<размер>`

## Part 11. Использование утилиты df

- **Вывод df:**
  - ![Скриншот `df`](#)
  - Для `/`: размер, занято, свободно, процент использования: `<значения>`
  - Единица измерения: `<KB/MB/...>`
- **Вывод df -Th:**
  - ![Скриншот `df -Th`](#)
  - Для `/`: размер, занято, свободно, процент использования, тип файловой системы: `<значения>`

## Part 12. Использование утилиты du

- **Вывод du:**
  - ![Скриншот `du`](#)
- **Размеры папок:**
  - `/home`, `/var`, `/var/log` в байтах и человекочитаемом виде: ![...](#)
- **Размер каждого элемента в /var/log:**
  - ![Скриншот `du /var/log/*`](#)

## Part 13. Установка и использование утилиты ncdu

- **Установка:**
  - `sudo apt install ncdu`.
- **Вывод ncdu:**
  - Размеры `/home`, `/var`, `/var/log`: ![...](#)

## Part 14. Работа с системными журналами

- **Просмотр логов:**
  - `/var/log/dmesg`, `/var/log/syslog`, `/var/log/auth.log`: ![...](#)
- **Последняя успешная авторизация:**
  - Время: `<время>`, пользователь: `<имя>`, метод: `<метод>`
- **Перезапуск sshd:**
  - ![Скриншот сообщения о рестарте в логах](#)

## Part 15. Использование планировщика заданий CRON

- **Запуск uptime каждые 2 минуты:**
  - Запись в crontab: `*/2 * * * * /usr/bin/uptime`.
- **Логи выполнения:**
  - ![Скриншот строк о выполнении из syslog](#)
- **Список текущих заданий CRON:**
  - ![Скриншот `crontab -l`](#)
- **Удаление всех заданий:**
  - `crontab -r`.
- **Проверка пустого списка:**
  - ![Скриншот пустого `crontab -l`](#)
